# To Do
- install quill to project
  - buat component `Editor` untuk Quill
- ganti export const quill ke export function agar seragam dengan component lainnya
- wrap component quill ke dalam form
- tambahkan listener untuk form tersebut dan arahkan ke `http://localhost:3000/api/notes` dengan
- tambahkan link di sidebar untuk menambah materi baru
- simpan materi ke DB
- buat backend untuk simpan materi ke DB